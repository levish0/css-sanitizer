# 2026-08-09 0.4.0 release version

## Objective
- Promote the completed alpha.72/resource-guard hardening from `[Unreleased]`/package 0.3.0 to the breaking release version 0.4.0.

## User-Approved Scope
- The user explicitly requested a proper version bump after the hardening milestone.
- Selected 0.4.0 because public policy APIs and `NodeAction::Skip` behavior changed incompatibly.
- Keep the user-selected `lightningcss 1.0.0-alpha.72` dependency requirement unchanged.
- Validate the complete workspace and a crates.io publish dry-run, without actually publishing.

## Implementation Status
- Completed:
  - Package and lockfile root package version updated to 0.4.0.
  - README installation example updated to 0.4.0.
  - Changelog promoted from `[Unreleased]` to `[0.4.0] - 2026-08-09`.
  - Full test and package/publish dry-run verification.
- Partial:
  - None.
- Not started:
  - Actual crates.io publish, git commit/tag, push, or GitHub release.

## Major Changes
- Files/modules:
  - `Cargo.toml`: `css-sanitizer` package version `0.3.0` -> `0.4.0`.
  - `Cargo.lock`: workspace package entry updated to `0.4.0`.
  - `README.md`: dependency example updated to `css-sanitizer = "0.4.0"`.
  - `CHANGELOG.md`: release heading is now `0.4.0` dated 2026-08-09.
- API routes:
  - No HTTP/API route changes; this milestone only finalized the Rust crate release version for the previously completed public API changes.
- DTO/repository/service/permission layers:
  - No new changes beyond the preceding resource-guard milestone.
- Worker/background jobs:
  - None.
- Migrations/entities/constants:
  - No database migrations or runtime constants; package metadata only.

## Validation
- Commands run:
  - `cargo metadata --no-deps --format-version 1`
  - `cargo test --workspace --all-targets`
  - `cargo publish --dry-run --allow-dirty -p css-sanitizer`
- Result:
  - Cargo metadata reports `css-sanitizer 0.4.0`.
  - All 87 integration tests and all-target benchmark smoke cases passed as version 0.4.0.
  - Publish dry-run packaged 37 files, verified the packaged crate successfully, reached the simulated upload step, and aborted upload as expected because it was a dry run.
- Skipped checks:
  - No actual publish, git commit, tag, push, or release creation.

## Remaining Work
- Known gaps:
  - None for the version-bump request.
- Risks:
  - Publishing remains an explicit future action; do not infer authorization from this dry-run milestone.
  - The lightningcss manifest requirement remains the user-selected non-exact alpha.72 requirement; dependency upgrades still require AST/resource re-audit.
- Suggested next entry points:
  - Commit/tag 0.4.0 if requested.
  - Run `cargo xtask publish` only after explicit user approval to actually publish.

## Notes For Next Agent
- This entry supersedes only the package-version/release-state statements in `2026-08-09-lightningcss-alpha72-resource-guard.md`; do not rewrite that historical entry.
- Current intended release version is 0.4.0 and the changelog is no longer `[Unreleased]`.
- The publish command was dry-run only; nothing was uploaded.
